// import { convertDurationToMillis } from '../../utils/DurationUnitConverter';
//
// export function useAnimationDurationDetection<E extends HTMLElement>(element: E, cssVariableName: string, fallbackValue: number = 0) {
//   const cs = window.getComputedStyle(element);
//   const rawValue = cs.getPropertyValue(cssVariableName);
//   return convertDurationToMillis(rawValue, fallbackValue);
// }
